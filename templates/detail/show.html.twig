{% extends 'base.html.twig' %}

{% block title %}Detail{% endblock %}

{% block body %}
    <h1>Detail</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ detail.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ detail.quantite }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ detail.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('detail_index') }}"><i class="fa fa-reply"></i> Retour à la liste</a>

    <a href="{{ path('detail_edit', {'id': detail.id}) }}"><i class="fa fa-edit"></i></a>

    {{ include('detail/_delete_form.html.twig') }}
{% endblock %}
